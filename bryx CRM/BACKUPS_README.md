# Система резервного копирования bryx CRM

## Обзор

bryx CRM использует **гибридную систему бэкапов** с двумя провайдерами:

1. **Railway Volume Provider** - для Railway окружения (Production)
2. **Local Provider** - для локальной разработки

Система **автоматически определяет** окружение и выбирает подходящий провайдер.

---

## Архитектура

### Railway Volume Provider

**Как работает:**
- Создаёт бэкапы через `pg_dump`
- Сохраняет файлы в **Railway Volume** (персистентное хранилище)
- Railway автоматически создаёт резервные копии самого Volume
- **Двойная защита**: файл бэкапа + автоматический бэкап Volume

**Преимущества:**
- ✅ Автоматическое резервирование Railway
- ✅ Не требует дополнительных сервисов (S3, GCS)
- ✅ Простая настройка
- ✅ Бэкапы сохраняются даже при пересборке контейнера

### Local Provider

**Как работает:**
- Использует `pg_dump` и `pg_restore`
- Сохраняет файлы в папку `Backups/` локально
- Требует установки PostgreSQL утилит

**Требования:**
- PostgreSQL установлен локально
- `pg_dump` и `pg_restore` доступны в PATH

---

## Настройка для Railway

### Шаг 1: Создание Volume

1. Откройте проект в **Railway Dashboard**
2. Перейдите в раздел **"Volumes"**
3. Нажмите **"New Volume"**
4. Укажите:
   - **Name**: `bryx-crm-backups`
   - **Size**: минимум **1GB** (рекомендуется 5GB+)

### Шаг 2: Монтирование Volume

1. Откройте настройки вашего **сервиса** (bryx CRM)
2. В разделе **"Volumes"** выберите созданный volume
3. Укажите **Mount Path**: `/app/backups`
4. Нажмите **"Save"**

Railway автоматически создаст переменную окружения:
```
RAILWAY_VOLUME_MOUNT_PATH=/app/backups
```

### Шаг 3: Проверка

После деплоя:
1. Откройте приложение
2. Перейдите на страницу `/backups`
3. В карточке "Информация" должно быть:
   - **Провайдер**: `Railway Volume` (зелёный badge)
   - **Описание**: "Бэкапы в Railway Volume с автоматическим бэкапом..."

4. Создайте тестовый бэкап
5. В списке бэкапов в колонке "Источник" должно быть `Railway Volume`

---

## Настройка для локальной разработки

### Шаг 1: Установка PostgreSQL

**Windows:**
```bash
# Скачайте PostgreSQL installer с https://www.postgresql.org/download/windows/
# Или через Chocolatey:
choco install postgresql
```

**macOS:**
```bash
brew install postgresql
```

**Linux:**
```bash
sudo apt-get install postgresql-client
```

### Шаг 2: Проверка установки

```bash
pg_dump --version
pg_restore --version
```

### Шаг 3: Конфигурация

В `appsettings.json` уже настроено:
```json
"BackupOptions": {
  "BackupDirectory": "Backups",
  "MaxBackupsToKeep": 10,
  "CompressBackups": true,
  "AutoBackupEnabled": true,
  "AutoBackupIntervalHours": 24
}
```

### Шаг 4: Проверка

1. Запустите приложение локально
2. Откройте `/backups`
3. Провайдер должен быть `Local (pg_dump)`
4. Создайте бэкап - файл появится в папке `Backups/`

---

## Использование

### Создание бэкапа

1. Откройте страницу **"Резервные копии"** (`/backups`)
2. Нажмите кнопку **"Создать бекап"**
3. Дождитесь завершения (индикатор загрузки)
4. Успешное сообщение подтвердит создание

### Восстановление из бэкапа

⚠️ **ВНИМАНИЕ**: Восстановление удалит все текущие данные!

1. В списке бэкапов найдите нужный
2. Нажмите кнопку **"Восстановить"**
3. Прочитайте предупреждение
4. Подтвердите восстановление
5. Дождитесь завершения

### Удаление бэкапа

1. В списке бэкапов нажмите кнопку **🗑️** (корзина)
2. Бэкап будет немедленно удалён

### Автоматические бэкапы

Система автоматически создаёт бэкапы каждые 24 часа (настраивается).

**Настройка интервала:**
```json
"AutoBackupIntervalHours": 12  // бэкап каждые 12 часов
```

**Отключение автобэкапов:**
```json
"AutoBackupEnabled": false
```

---

## Мониторинг и управление

### Railway Dashboard

#### Просмотр Volume:
1. Откройте ваш проект
2. Перейдите в **"Volumes"**
3. Выберите `bryx-crm-backups`
4. Здесь вы увидите:
   - Общий размер
   - Используемое место
   - История бэкапов Volume (автоматические от Railway)

#### Восстановление Volume:
1. В разделе Volume нажмите **"Backups"**
2. Выберите точку восстановления
3. Нажмите **"Restore"**

### Через приложение

Вся информация доступна на странице `/backups`:
- Текущий провайдер
- Количество бэкапов
- Общий размер
- Последний бэкап
- Список всех бэкапов с фильтрацией по провайдеру

---

## Технические детали

### Формат бэкапов

Railway Provider:
```
railway_backup_{database}_{timestamp}.sql
Пример: railway_backup_bryx_crm_20251213_154530.sql
```

Local Provider:
```
backup_{database}_{timestamp}.sql
Пример: backup_bryx_crm_20251213_154530.sql
```

### Автоочистка

Система автоматически удаляет старые бэкапы:
- Хранится максимум `MaxBackupsToKeep` бэкапов (по умолчанию 10)
- Старые бэкапы удаляются после создания нового
- Очистка происходит по дате создания (FIFO)

### Логирование

Все операции логируются:
```
🔧 Выбран провайдер бэкапов: Railway Volume
🔄 [Railway] Начинаем создание бекапа в Volume: bryx_crm
✅ [Railway] Бекап успешно создан в Volume: railway_backup_...
🛡️ [Railway] Volume будет автоматически забэкаплен Railway
```

---

## Поиск и устранение неисправностей

### Ошибка: "Ни один провайдер бэкапов не доступен"

**Причина**: Не настроен ни один провайдер

**Решение для Railway:**
1. Проверьте, что Volume создан и примонтирован
2. Проверьте переменную `RAILWAY_VOLUME_MOUNT_PATH`
3. Проверьте логи: есть ли сообщение о выборе провайдера

**Решение для Local:**
1. Установите PostgreSQL
2. Убедитесь, что `pg_dump` в PATH:
   ```bash
   where pg_dump    # Windows
   which pg_dump    # macOS/Linux
   ```

### Ошибка: "pg_dump завершился с ошибкой"

**Проверьте:**
1. Доступность базы данных
2. Правильность `DATABASE_URL` / connection string
3. Права доступа к папке бэкапов
4. Свободное место на диске/Volume

### Бэкапы не создаются автоматически

**Проверьте:**
1. `AutoBackupEnabled` = `true` в настройках
2. Логи на наличие ошибок от `AutoBackupHostedService`
3. Интервал `AutoBackupIntervalHours` (по умолчанию 24 часа)

### Volume заполнен

**Решение:**
1. Уменьшите `MaxBackupsToKeep`
2. Удалите старые бэкапы вручную
3. Увеличьте размер Volume в Railway Dashboard

---

## Рекомендации

### Для Production (Railway):

✅ **Рекомендуется:**
- Размер Volume: минимум 5GB
- `MaxBackupsToKeep`: 7-14 (неделя-две истории)
- `AutoBackupIntervalHours`: 6-24 (4 раза в день - 1 раз в день)
- Регулярно проверяйте Railway Volume Backups

❌ **Не рекомендуется:**
- Отключать автобэкапы в production
- Хранить менее 3 бэкапов
- Использовать Volume меньше 1GB

### Для Development:

✅ **Рекомендуется:**
- Регулярное тестирование процесса восстановления
- Создание бэкапов перед большими изменениями схемы БД

---

## Дополнительная защита

### Тройная защита на Railway:

1. **Файлы бэкапов** в Volume (`pg_dump`)
2. **Railway Volume Backups** (автоматические снапшоты Volume)
3. **Railway Database Backups** (встроенные бэкапы PostgreSQL)

### Доступ к Railway Database Backups:

1. Откройте PostgreSQL сервис в Railway
2. Перейдите в **"Backups"** tab
3. Здесь вы можете:
   - Создать manual backup
   - Настроить schedule
   - Восстановить из точки восстановления

---

## FAQ

**Q: Чем отличаются Railway Volume Backups от бэкапов через pg_dump?**

A:
- **pg_dump** - создаёт SQL файл с данными и схемой
- **Volume Backups** - снапшот всей файловой системы Volume
- **Railway DB Backups** - встроенные Point-in-Time Recovery бэкапы

Все три метода дополняют друг друга.

**Q: Можно ли использовать оба провайдера одновременно?**

A: Нет, система автоматически выбирает один провайдер. Но при отображении списка бэкапов показываются файлы от всех провайдеров.

**Q: Куда сохраняются бэкапы на Railway?**

A: В Volume по пути `/app/backups` внутри контейнера. Volume персистентен между деплоями.

**Q: Сколько стоит Railway Volume?**

A: Railway взимает плату за используемое место в Volume. Проверьте текущие тарифы на https://railway.com/pricing

---

## Поддержка

При возникновении проблем:
1. Проверьте логи приложения
2. Убедитесь, что Volume правильно настроен
3. Проверьте переменные окружения
4. Обратитесь к документации Railway: https://docs.railway.com/reference/volumes
