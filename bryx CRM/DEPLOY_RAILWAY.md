# Инструкция по деплою bryx CRM на Railway

## Подготовка

1. Зарегистрируйтесь на [Railway](https://railway.app) (можно через GitHub)
2. Установите Git, если еще не установлен

## Шаг 1: Инициализация Git репозитория

Откройте терминал в папке проекта и выполните:

```bash
cd "C:\Users\BRYX\Desktop\bryx_base\bryx CRM"
git init
git add .
git commit -m "Initial commit: bryx CRM ready for deployment"
```

## Шаг 2: Создание проекта на Railway

### Вариант А: Через веб-интерфейс (проще)

1. Зайдите на https://railway.app
2. Нажмите **"New Project"**
3. Выберите **"Deploy from GitHub repo"** (если хотите использовать GitHub)
   - Или выберите **"Empty Project"** для ручного деплоя

### Вариант Б: Через Railway CLI (быстрее)

```bash
# Установите Railway CLI
npm i -g @railway/cli

# Залогиньтесь
railway login

# Создайте новый проект
railway init

# Подключите PostgreSQL
railway add --plugin postgresql

# Задеплойте приложение
railway up
```

## Шаг 3: Добавление PostgreSQL базы данных

1. В вашем Railway проекте нажмите **"+ New"**
2. Выберите **"Database"**
3. Выберите **"Add PostgreSQL"**
4. Railway автоматически создаст переменную окружения `DATABASE_URL`

## Шаг 4: Настройка проекта

В настройках вашего сервиса (Service Settings):

1. Убедитесь, что **Root Directory** указывает на `bryx CRM` (если нужно)
2. Добавьте переменные окружения (если требуется):
   - `ASPNETCORE_ENVIRONMENT=Production`

## Шаг 5: Деплой

### Если используете GitHub:

1. Создайте репозиторий на GitHub
2. Добавьте remote:
   ```bash
   git remote add origin https://github.com/ВАШ_USERNAME/bryx-crm.git
   git branch -M main
   git push -u origin main
   ```
3. Railway автоматически задеплоит при каждом push

### Если используете Railway CLI:

```bash
# Находясь в папке проекта
railway up
```

## Шаг 6: Проверка деплоя

1. После деплоя Railway предоставит URL вашего приложения
2. Откройте URL в браузере
3. Войдите с учетными данными:
   - **Email:** admin@bryx.com
   - **Пароль:** admin123

## Важные моменты

### Автоматическое применение миграций

Приложение автоматически создает администратора при запуске. База данных будет автоматически мигрирована.

### Проверка логов

Если что-то пошло не так, проверьте логи:

```bash
railway logs
```

Или в веб-интерфейсе Railway в разделе **Deployments** → **View Logs**

### Переменные окружения

Railway автоматически предоставляет:
- `DATABASE_URL` - строка подключения к PostgreSQL
- `PORT` - порт для приложения

Приложение уже настроено для работы с этими переменными.

## Обновление приложения

### Через Git:

```bash
git add .
git commit -m "Описание изменений"
git push
```

Railway автоматически пересоберет и задеплоит.

### Через Railway CLI:

```bash
railway up
```

## Troubleshooting

### Ошибка при старте

Проверьте логи:
```bash
railway logs
```

### База данных не подключается

Убедитесь, что:
1. PostgreSQL плагин добавлен
2. Переменная `DATABASE_URL` установлена
3. В логах нет ошибок подключения

### Приложение не отвечает

Проверьте, что:
1. Порт правильно настроен (используется переменная `PORT`)
2. Приложение слушает на `0.0.0.0`, а не `localhost`

## Дополнительные ресурсы

- [Railway Documentation](https://docs.railway.app)
- [Railway Discord](https://discord.gg/railway)

## Быстрая команда для деплоя (если все настроено)

```bash
# Коммит изменений
git add . && git commit -m "Update" && git push

# Или через Railway CLI
railway up
```

Готово! Ваш bryx CRM теперь доступен онлайн! 🚀
