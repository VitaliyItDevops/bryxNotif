# Быстрый деплой на Railway 🚀

## Что я сделал для подготовки к деплою:

✅ Настроил приложение для чтения переменных окружения Railway
✅ Добавил поддержку DATABASE_URL от Railway
✅ Настроил динамический PORT
✅ Добавил автоматическое применение миграций при старте
✅ Создал .gitignore для исключения ненужных файлов
✅ Приложение готово к деплою!

---

## Самый быстрый способ - Railway CLI

### Шаг 1: Установка Railway CLI
```bash
npm i -g @railway/cli
```

### Шаг 2: Инициализация Git (если еще не сделано)
```bash
cd "C:\Users\BRYX\Desktop\bryx_base\bryx CRM"
git init
git add .
git commit -m "Initial commit: Ready for deployment"
```

### Шаг 3: Деплой
```bash
# Залогиниться в Railway
railway login

# Создать новый проект
railway init

# Добавить PostgreSQL
railway add --plugin postgresql

# Задеплоить приложение
railway up
```

### Шаг 4: Открыть приложение
```bash
railway open
```

Railway автоматически даст вам публичный URL!

---

## Альтернатива - через веб-интерфейс

### Шаг 1: Создайте GitHub репозиторий

1. Зайдите на https://github.com
2. Создайте новый репозиторий (например, `bryx-crm`)
3. **НЕ** добавляйте README, .gitignore или лицензию

### Шаг 2: Загрузите код на GitHub

```bash
cd "C:\Users\BRYX\Desktop\bryx_base\bryx CRM"
git init
git add .
git commit -m "Initial commit: Ready for deployment"
git branch -M main
git remote add origin https://github.com/ВАШ_USERNAME/bryx-crm.git
git push -u origin main
```

### Шаг 3: Деплой на Railway

1. Зайдите на https://railway.app
2. Нажмите **"Start a New Project"**
3. Выберите **"Deploy from GitHub repo"**
4. Выберите ваш репозиторий `bryx-crm`
5. Railway автоматически определит .NET проект

### Шаг 4: Добавьте PostgreSQL

1. В вашем проекте нажмите **"+ New"**
2. Выберите **"Database"** → **"Add PostgreSQL"**
3. Railway автоматически свяжет базу с вашим приложением

### Шаг 5: Настройки (опционально)

В **Service Settings** вашего приложения:
- **Root Directory**: оставьте пустым или укажите `bryx CRM`
- **Start Command**: оставьте пустым (Railway определит автоматически)

### Шаг 6: Деплой!

Railway автоматически задеплоит приложение. Через 2-3 минуты вы увидите ссылку на ваше приложение.

---

## Вход в систему

После деплоя используйте эти данные для входа:

- **Email:** `admin@bryx.com`
- **Пароль:** `admin123`

---

## Проверка деплоя

После деплоя откройте URL от Railway в браузере. Вы должны увидеть страницу входа.

Если что-то не работает, проверьте логи:
```bash
railway logs
```

Или в веб-интерфейсе: **Deployments** → **View Logs**

---

## Обновление приложения

### Через Git:
```bash
git add .
git commit -m "Описание изменений"
git push
```

Railway автоматически пересоберет и задеплоит!

### Через Railway CLI:
```bash
railway up
```

---

## Важные замечания

1. **PostgreSQL обязателен** - Railway предоставит DATABASE_URL автоматически
2. **Миграции применяются автоматически** при старте приложения
3. **Администратор создается автоматически** при первом запуске
4. **SSL для базы данных** уже настроен

---

## Полная инструкция

Для детальной инструкции смотрите файл **DEPLOY_RAILWAY.md**

---

## Нужна помощь?

- Railway Docs: https://docs.railway.app
- Railway Discord: https://discord.gg/railway

**Готово! Ваше приложение готово к деплою! 🎉**
