# Bryx Telegram Bot

Telegram бот для управления CRM системой Bryx.

## Возможности

- 📦 Просмотр товаров и их наличия
- 💰 Просмотр продаж
- 📊 Получение статистики по складу и продажам
- 🔍 Фильтрация по категориям
- ⚡ Быстрый доступ к информации

## Установка и настройка

### 1. Создание Telegram бота

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Сохраните полученный токен (выглядит как `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

### 2. Настройка конфигурации

Откройте файл `appsettings.json` и укажите:

```json
{
  "BotConfiguration": {
    "BotToken": "ВАШ_ТОКЕН_БОТА",
    "CrmApiUrl": "http://localhost:5092/api/bot"
  }
}
```

**Важно:**
- `BotToken` - токен, полученный от BotFather
- `CrmApiUrl` - URL API вашей CRM (по умолчанию `http://localhost:5092/api/bot`)

### 3. Запуск CRM

Сначала убедитесь, что CRM запущена:

```bash
cd "bryx CRM/bryx CRM"
dotnet run
```

CRM будет доступна по адресу `http://localhost:5092`

### 4. Запуск бота

```bash
cd bryxBot/BryxBot
dotnet run
```

Вы должны увидеть сообщение:
```
Запуск Bryx Bot...
Бот запущен: @ваш_бот
```

## Использование

### Доступные команды

- `/start` - Приветственное сообщение
- `/help` - Список всех команд
- `/menu` - Главное меню с кнопками
- `/products` - Просмотр товаров
- `/sales` - Просмотр продаж
- `/stats` - Статистика по CRM

### Примеры использования

1. **Просмотр товаров**
   - Отправьте `/products`
   - Бот покажет список товаров из CRM

2. **Просмотр статистики**
   - Отправьте `/stats`
   - Получите сводку по товарам и продажам

3. **Использование меню**
   - Отправьте `/menu`
   - Используйте кнопки для быстрого доступа

## API Endpoints

Бот использует следующие endpoints CRM:

- `GET /api/bot/products` - Список товаров
- `GET /api/bot/products/{id}` - Детали товара
- `GET /api/bot/sales` - Список продаж
- `GET /api/bot/sales/{id}` - Детали продажи
- `GET /api/bot/stats` - Статистика
- `GET /api/bot/categories` - Категории товаров

### Примеры запросов

```bash
# Получить товары
curl http://localhost:5092/api/bot/products

# Получить товары по категории
curl http://localhost:5092/api/bot/products?category=Наушники

# Получить статистику
curl http://localhost:5092/api/bot/stats
```

## Структура проекта

```
BryxBot/
├── Program.cs              # Точка входа
├── BotService.cs           # Основной сервис бота
├── MessageHandler.cs       # Обработчик команд
├── BotConfiguration.cs     # Модель конфигурации
└── appsettings.json        # Файл настроек
```

## Разработка

### Добавление новой команды

1. Откройте `MessageHandler.cs`
2. Добавьте обработку новой команды в метод `HandleMessage`:

```csharp
"/mynewcommand" => HandleMyNewCommand(botClient, chatId, cancellationToken),
```

3. Создайте метод обработчик:

```csharp
private async Task HandleMyNewCommand(ITelegramBotClient botClient, long chatId, CancellationToken cancellationToken)
{
    await botClient.SendMessage(
        chatId: chatId,
        text: "Текст ответа",
        cancellationToken: cancellationToken
    );
}
```

### Интеграция с CRM API

Для запросов к CRM используйте `_httpClient` в `MessageHandler`:

```csharp
var response = await _httpClient.GetAsync("/products");
var data = await response.Content.ReadFromJsonAsync<ProductsResponse>();
```

## Troubleshooting

### Бот не отвечает

1. Проверьте, что CRM запущена и доступна
2. Убедитесь, что токен бота указан правильно
3. Проверьте логи бота на наличие ошибок

### Ошибки подключения к API

1. Убедитесь, что `CrmApiUrl` в `appsettings.json` указан правильно
2. Проверьте, что CRM запущена на указанном порту
3. Попробуйте открыть `http://localhost:5092/api/bot/stats` в браузере

### Бот не стартует

1. Проверьте, что у вас установлен .NET 9.0 SDK
2. Убедитесь, что все пакеты установлены: `dotnet restore`
3. Проверьте логи на наличие ошибок

## Требования

- .NET 9.0 SDK
- Запущенная CRM система Bryx
- Токен Telegram бота от BotFather

## Лицензия

Проект является частью Bryx CRM системы.
